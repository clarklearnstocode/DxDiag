<?php
/**
 * BookingController
 * All booking logic has been consolidated into PropertyController.
 * This class is kept to avoid "class not found" errors from index.php's require_once.
 */
class BookingController {
    // Intentionally empty — see PropertyController for all booking methods:
    // confirmBooking(), showBookingConfirmation(), cancelBooking(),
    // showEditBooking(), updateBooking()
}
